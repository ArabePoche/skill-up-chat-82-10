/**
 * Enregistrement du Service Worker avec vite-plugin-pwa
 * Le service worker est généré automatiquement par le plugin
 */

export const registerServiceWorker = async () => {
  // vite-plugin-pwa gère l'enregistrement automatiquement
  console.log('✅ PWA Service Worker will be registered automatically');
};
